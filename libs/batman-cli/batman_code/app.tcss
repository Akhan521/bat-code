/* bat-code — Batman color palette (Phase 1 minimal) */

Screen {
    background: #0a0a0f;
}

#placeholder {
    width: 100%;
    height: 100%;
    content-align: center middle;
    color: #f5c518;
    background: #0a0a0f;
}
